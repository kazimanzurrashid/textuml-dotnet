
define(function() {
  var Participant;
  return Participant = (function() {

    function Participant(name, alias) {
      this.name = name;
      this.alias = alias;
    }

    return Participant;

  })();
});
